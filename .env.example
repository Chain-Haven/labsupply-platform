# ============================================================================
# WhiteLabel Peptides Platform - Environment Variables
# ============================================================================

# Supabase (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application URLs
# NEXT_PUBLIC_APP_URL is the canonical origin for all auth redirects.
# In production, set to your real domain (e.g. https://whitelabel.peptidetech.co).
NEXT_PUBLIC_APP_URL=http://localhost:3000
APP_BASE_URL=http://localhost:3001

# Mercury Banking API (REQUIRED for invoicing)
MERCURY_API_TOKEN=your-mercury-api-token
# Optional: Override auto-discovered deposit account
# MERCURY_ACCOUNT_ID=your-mercury-checking-account-id
# Optional: Set via Admin > Mercury > Setup Webhook button
# MERCURY_WEBHOOK_SECRET=your-mercury-webhook-secret

# Store Secret Encryption (REQUIRED for WooCommerce HMAC auth)
STORE_SECRET_ENCRYPTION_KEY=generate-a-64-char-hex-string

# CORS (comma-separated allowed origins)
ALLOWED_ORIGINS=https://your-production-domain.vercel.app

# Admin Configuration
NEXT_PUBLIC_SUPER_ADMIN_EMAIL=admin@yourdomain.com

# Workflow Engine - Inngest (REQUIRED for cron jobs)
INNGEST_EVENT_KEY=your-inngest-event-key
INNGEST_SIGNING_KEY=your-inngest-signing-key

# SMTP - Email Notifications (Optional, needed for backup codes + merchant notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@yourdomain.com

# BTC Wallet (Required for BTC deposits)
BTC_XPUB_ENCRYPTION_KEY=generate-a-64-char-hex-string-for-xpub-encryption
ESPLORA_BASE_URL=https://blockstream.info/api
BTC_NETWORK=mainnet

# Shipping Provider (shipstation | easypost | stub)
SHIPPING_PROVIDER=shipstation
SHIPSTATION_API_KEY=your-shipstation-api-key
SHIPSTATION_API_SECRET=your-shipstation-api-secret
# Legacy EasyPost (optional)
EASYPOST_API_KEY=your-easypost-api-key

# Error Tracking (Optional)
SENTRY_DSN=your-sentry-dsn

# QStash for webhook buffering (Optional)
QSTASH_URL=https://qstash.upstash.io
QSTASH_TOKEN=your-qstash-token
